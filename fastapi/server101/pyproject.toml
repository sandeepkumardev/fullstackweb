[project]
name = "server101"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sandeep k.", email = "buildbysandeep@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[tool.fastapi]
entrypoint = "server101.main:app"

[project.scripts]
dev = "server101.main:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
